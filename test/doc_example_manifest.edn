{:version 1,
 :statuses #{:api-sketch :fragment :external :runnable},
 :fixtures
 {:reading-data-empty
  {:setup
   "(require '[datalevin.core :as d])\n(def schema {:user/email {:db/valueType :db.type/string :db/unique :db.unique/identity} :user/name {:db/valueType :db.type/string} :user/friends {:db/valueType :db.type/ref :db/cardinality :db.cardinality/many} :order/id {:db/valueType :db.type/string :db/unique :db.unique/identity} :order/customer {:db/valueType :db.type/ref}})\n(def conn (d/create-conn nil schema {:kv-opts {:inmemory? true}}))",
   :teardown "(when-let [v (resolve 'conn)] (d/close @v))"},
  :reading-data
  {:setup
   "(require '[datalevin.core :as d])\n(require '[clojure.string])\n(def schema {:user/email {:db/valueType :db.type/string :db/unique :db.unique/identity} :user/name {:db/valueType :db.type/string} :user/friends {:db/valueType :db.type/ref :db/cardinality :db.cardinality/many} :order/id {:db/valueType :db.type/string :db/unique :db.unique/identity} :order/customer {:db/valueType :db.type/ref}})\n(def conn (d/create-conn nil schema {:kv-opts {:inmemory? true}}))\n(d/transact! conn [{:user/email \"alice@example.com\" :user/name \"Alice\"} {:user/email \"bob@example.com\" :user/name \"Bob\" :user/friends [[:user/email \"alice@example.com\"]]} {:order/id \"o-1001\" :order/customer [:user/email \"alice@example.com\"]}])\n(def db (d/db conn))",
   :teardown "(when-let [v (resolve 'conn)] (d/close @v))"}},
 :generated-by "datalevin.docs.example-verify/write-skeleton!",
 :examples
 {"01-why-datalevin#clojure-001"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "01-why-datalevin.md:218",
   :preview
   "[:find ?z :in $ ?x :where [?x :person/follows ?y] [?y :person/follows ?z]]"},
  "01-why-datalevin#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "01-why-datalevin.md:500",
   :preview
   "[:find ?order-id ?customer-email ?product-name :in $ ?vendor ?region ?since :where [?vendo"},
  "01-why-datalevin#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "01-why-datalevin.md:712",
   :preview
   "(require '[datalevin.core :as d]) ;; Schema is optional, but this example declares it for "},
  "02-getting-started#clojure-001"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "02-getting-started.md:38",
   :preview
   ";; deps.edn {:deps {org.datalevin/datalevin-embedded {:mvn/version \"{{datalevin-version}}\""},
  "02-getting-started#clojure-002"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "02-getting-started.md:77",
   :preview
   ";; deps.edn {:aliases {:datalevin/jvm {:jvm-opts [\"--add-opens=java.base/java.nio=ALL-UNNA"},
  "02-getting-started#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "02-getting-started.md:118",
   :preview
   "(require '[datalevin.core :as d]) (def db-path \"/tmp/datalevin-book-getting-started\") (def"},
  "02-getting-started#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "02-getting-started.md:192",
   :preview "(def conn (d/get-conn db-path schema))"},
  "02-getting-started#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "02-getting-started.md:223",
   :preview
   "(d/transact! conn [{:user/name \"Alice\" :user/age 30 :user/city \"Paris\"} {:user/name \"Bob\" "},
  "02-getting-started#clojure-006"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "02-getting-started.md:265",
   :preview
   "[10001 :user/name \"Alice\"] [10001 :user/age 30] [10001 :user/city \"Paris\"] [10002 :user/na"},
  "02-getting-started#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "02-getting-started.md:294",
   :preview
   "(d/q '[:find [?name ...] :where [?e :user/name ?name] [?e :user/age ?age] [(> ?age 28)]] ("},
  "02-getting-started#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "02-getting-started.md:382",
   :preview
   "(d/pull (d/db conn) [:user/name :user/age :user/city] [:user/name \"Alice\"]) ;; => {:user/n"},
  "02-getting-started#clojure-009"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "02-getting-started.md:422",
   :preview "(d/close conn)"},
  "02-getting-started#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "02-getting-started.md:445",
   :preview
   "(def conn2 (d/get-conn db-path schema)) (d/q '[:find [?city ...] :where [?e :user/name \"Bo"},
  "02-getting-started#clojure-011"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "02-getting-started.md:510",
   :preview
   "(def mem-conn (d/create-conn nil schema {:kv-opts {:inmemory? true}}))"},
  "03-mental-model#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "03-mental-model.md:171",
   :preview
   ";; The schema defines the behavior of attributes, not the shape of rows. (def schema {:per"},
  "03-mental-model#clojure-002"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "03-mental-model.md:291",
   :preview
   "[1001 :customer/email \"alice@example.com\"] [2001 :order/number \"A-100\"] [2001 :order/custo"},
  "03-mental-model#clojure-003"
  {:status :runnable,
   :reason "Runs against a small in-memory user fixture.",
   :source "03-mental-model.md:429",
   :preview
   "(d/q '[:find ?email :where [?e :user/name \"Alice\"] [?e :user/email ?email]] db)",
   :setup
   "(require '[datalevin.core :as d])\n(def conn (d/create-conn nil {:user/name {:db/valueType :db.type/string} :user/email {:db/valueType :db.type/string}} {:kv-opts {:inmemory? true}}))\n(d/transact! conn [{:user/name \"Alice\" :user/email \"alice@example.com\"}])\n(def db (d/db conn))",
   :teardown "(when-let [v (resolve 'conn)] (d/close @v))"},
  "03-mental-model#clojure-004"
  {:status :runnable,
   :reason "Runs against a small in-memory person fixture.",
   :source "03-mental-model.md:473",
   :preview
   "(d/q '[:find ?attr :where [?p :person/name] [?p ?attr]] db)",
   :setup
   "(require '[datalevin.core :as d])\n(def conn (d/create-conn nil {:person/name {:db/valueType :db.type/string} :person/email {:db/valueType :db.type/string}} {:kv-opts {:inmemory? true}}))\n(d/transact! conn [{:person/name \"Alice\" :person/email \"alice@example.com\"}])\n(def db (d/db conn))",
   :teardown "(when-let [v (resolve 'conn)] (d/close @v))"},
  "03-mental-model#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "03-mental-model.md:506",
   :preview
   ";; A hybrid query combines text search with exact metadata filters. (d/q '[:find ?title :w"},
  "03-mental-model#clojure-006"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "03-mental-model.md:565",
   :preview
   "(def user-rules '[[(premium-user ?u) [?u :user/status :user.status/active] [?sub :subscrip"},
  "03-mental-model#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "03-mental-model.md:647",
   :preview
   "(def ancestry-rules '[[(ancestor ?x ?y) ; Rule name and arguments [?x :parent ?y]] ; Case "},
  "03-mental-model#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "03-mental-model.md:694",
   :preview
   "(d/q '[:find [?ancestor ...] :in $ % :where (ancestor 42 ?ancestor)] db ancestry-rules)"},
  "05-attributes-entities-namespaces#clojure-001"
  {:status :runnable,
   :reason "Runs against a fresh in-memory connection.",
   :source "05-attributes-entities-namespaces.md:43",
   :preview
   ";; This works even if :user/name was never defined (d/transact! conn [{:user/name \"Alice\"}",
   :setup
   "(require '[datalevin.core :as d])\n(def conn (d/create-conn nil {} {:kv-opts {:inmemory? true}}))",
   :teardown "(when-let [v (resolve 'conn)] (d/close @v))"},
  "05-attributes-entities-namespaces#clojure-002"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "05-attributes-entities-namespaces.md:152",
   :preview
   "(def schema {:user/email {:db/valueType :db.type/string :db/unique :db.unique/identity} :u"},
  "05-attributes-entities-namespaces#clojure-003"
  {:status :runnable,
   :reason
   "Runs against an in-memory connection with cardinality-many tags.",
   :source "05-attributes-entities-namespaces.md:228",
   :preview
   ";; :user/tags is :db.cardinality/many (d/transact! conn [{:db/id 1 :user/tags \"clojure\"}])",
   :setup
   "(require '[datalevin.core :as d])\n(def conn (d/create-conn nil {:user/tags {:db/cardinality :db.cardinality/many}} {:kv-opts {:inmemory? true}}))",
   :teardown "(when-let [v (resolve 'conn)] (d/close @v))"},
  "05-attributes-entities-namespaces#clojure-004"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "05-attributes-entities-namespaces.md:301",
   :preview
   ":user/email ;; namespace: user, name: email :order/id ;; namespace: order, name: id :line-"},
  "05-attributes-entities-namespaces#clojure-005"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "05-attributes-entities-namespaces.md:339",
   :preview
   "{:user/email \"ada@example.com\" :account/id \"acct-1\" :account/plan :account.plan/pro}"},
  "05-attributes-entities-namespaces#clojure-006"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "05-attributes-entities-namespaces.md:406",
   :preview "{:order/status :order.status/paid}"},
  "05-attributes-entities-namespaces#clojure-007"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "05-attributes-entities-namespaces.md:467",
   :preview
   ":order/placed-at :invoice/issued-at :shipment/delivered-at :support-ticket/closed-at"},
  "05-attributes-entities-namespaces#clojure-008"
  {:status :runnable,
   :reason "Runs against an in-memory connection with a ref schema.",
   :source "05-attributes-entities-namespaces.md:590",
   :preview
   "(d/transact! conn [{:db/id -1 :user/name \"Alice\"} {:db/id -2 :user/name \"Bob\" :user/friend",
   :setup
   "(require '[datalevin.core :as d])\n(def conn (d/create-conn nil {:user/name {:db/valueType :db.type/string} :user/friend {:db/valueType :db.type/ref}} {:kv-opts {:inmemory? true}}))",
   :teardown "(when-let [v (resolve 'conn)] (d/close @v))"},
  "05-attributes-entities-namespaces#clojure-009"
  {:status :runnable,
   :reason "Runs against a fresh in-memory connection.",
   :source "05-attributes-entities-namespaces.md:629",
   :preview
   "(let [report (d/transact! conn [{:db/id -1 :user/name \"Alice\"}])] (get-in report [:tempids",
   :setup
   "(require '[datalevin.core :as d])\n(def conn (d/create-conn nil {} {:kv-opts {:inmemory? true}}))",
   :teardown "(when-let [v (resolve 'conn)] (d/close @v))"},
  "06-transactions#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:72",
   :preview
   "(def kv (d/datalog-kv conn)) ;; Force a synchronous flush to disk after a burst of faster "},
  "06-transactions#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:206",
   :preview
   "(let [preview (d/tx-data->simulated-report (d/db conn) [{:db/id -1 :user/email \"preview@ex"},
  "06-transactions#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:231",
   :preview
   "(let [report (d/transact! conn [{:db/id 101 :account/status :closed}])] {:before (d/pull ("},
  "06-transactions#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:250",
   :preview
   "(def report (d/transact! conn [{:db/id -1 :user/email \"alice@example.com\" :user/name \"Alic"},
  "06-transactions#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:316",
   :preview
   "(d/transact! conn [{:user/name \"Alice\" :user/email \"alice@example.com\"} {:user/name \"Bob\" "},
  "06-transactions#clojure-006"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:358",
   :preview "(d/transact! conn [{:db/id 101, :user/active? false}])"},
  "06-transactions#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:398",
   :preview
   "(require '[datalevin.core :as d]) (def conn (d/get-conn \"/tmp/entity-time-demo\" {:user/ema"},
  "06-transactions#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:519",
   :preview
   "(d/transact! conn [{:db/id -1, :user/name \"Alice\" :user/friend -2} {:db/id -2, :user/name "},
  "06-transactions#clojure-009"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:557",
   :preview
   "(d/transact! conn [{:db/id -1 :user/name \"Alice\" :user/friend -2} {:db/id -2 :user/name \"B"},
  "06-transactions#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:609",
   :preview
   "(d/transact! conn [[:db/add [:user/email \"alice@example.com\"] :user/active? false]])"},
  "06-transactions#clojure-011"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:648",
   :preview
   ";; First, add a schema with unique identity (d/transact! conn [{:db/ident :user/email :db/"},
  "06-transactions#clojure-012"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:714",
   :preview
   "(d/transact! conn [[:db/add -1 :user/name \"Alice\"] [:db/add -1 :user/email \"alice@example."},
  "06-transactions#clojure-013"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:758",
   :preview
   ";; Remove one known value. (d/transact! conn [[:db/retract [:user/email \"alice@example.com"},
  "06-transactions#clojure-014"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "06-transactions.md:847",
   :preview
   "{:order/id {:db/valueType :db.type/string :db/unique :db.unique/identity} :order/items {:d"},
  "06-transactions#clojure-015"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:911",
   :preview
   "(d/transact! conn [[:db/retractEntity [:order/id \"o-1001\"]]])"},
  "06-transactions#clojure-016"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:956",
   :preview
   "(d/transact! conn [{:user/email \"new@example.com\" :user/name \"New User\"} ; upsert [:db/add"},
  "06-transactions#clojure-017"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:1020",
   :preview
   ";; Transacting with an explicit int (d/transact! conn [{:db/id -1, :my-entity/val (int 42)"},
  "06-transactions#clojure-018"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:1058",
   :preview
   "(def listener-key (d/listen! conn :audit-log (fn [{:keys [tx-data tx-meta tempids]}] (prin"},
  "06-transactions#clojure-019"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:1155",
   :preview
   "(d/listen! conn :cache (fn [report] (invalidate-cache! report))) (d/listen! conn :cache (f"},
  "06-transactions#clojure-020"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:1189",
   :preview
   "(d/transact! conn [{:user/email \"ada@example.com\" :user/name \"Ada\"} {:event/type :event.ty"},
  "06-transactions#clojure-021"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:1256",
   :preview
   "(d/with-transaction [tx conn] (let [current-balance (d/q '[:find ?bal . :where [101 :accou"},
  "06-transactions#clojure-022"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:1317",
   :preview
   "(d/with-transaction [tx conn {:timeout-ms 5000}] (d/transact! tx [{:db/id 101 :account/che"},
  "06-transactions#clojure-023"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:1372",
   :preview "(def kv (d/datalog-kv conn))"},
  "06-transactions#clojure-024"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:1405",
   :preview
   "(let [kv (d/datalog-kv conn)] ;; DBI opening is idempotent, so this is safe during applica"},
  "06-transactions#clojure-025"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:1530",
   :preview
   "(ns my.app.payments (:require [datalevin.core :as d])) (defn account-open-and-unlocked? [d"},
  "06-transactions#clojure-026"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:1558",
   :preview
   "(d/transact! conn [[:db/cas [:user/email \"alice@example.com\"] :account/balance 100 75]])"},
  "06-transactions#clojure-027"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:1617",
   :preview
   "(require '[datalevin.udf :as udf]) (def descriptor {:udf/lang :java :udf/kind :tx-fn :udf/"},
  "06-transactions#clojure-028"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:1795",
   :preview
   "(defn rename-user [db email new-name] (if-let [eid (d/q '[:find ?e . :in $ ?email :where ["},
  "06-transactions#clojure-029"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:1824",
   :preview
   "(require '[datalevin.interpret :as i]) (def rename-user* (i/inter-fn [db email new-name] ("},
  "06-transactions#clojure-030"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "06-transactions.md:1843",
   :preview
   "(d/transact! conn [[:db.fn/call :user/rename \"bob@example.com\" \"Bob V3\"]])"},
  "07-reading-data#clojure-001"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "07-reading-data.md:74",
   :preview "[:user/email \"alice@example.com\"]"},
  "07-reading-data#clojure-002"
  {:status :runnable,
   :reason "Runs against an empty Chapter 7 in-memory fixture.",
   :source "07-reading-data.md:99",
   :preview
   "(def schema {:user/email {:db/valueType :db.type/string :db/unique :db.unique/identity} :u",
   :fixture :reading-data-empty},
  "07-reading-data#clojure-003"
  {:status :runnable,
   :reason "Runs against the shared Chapter 7 lookup-ref fixture.",
   :source "07-reading-data.md:227",
   :preview
   ";; Resolve to the numeric entity id when you explicitly need it. (d/entid db [:user/email ",
   :fixture :reading-data},
  "07-reading-data#clojure-004"
  {:status :runnable,
   :reason "Runs against the shared Chapter 7 order fixture.",
   :source "07-reading-data.md:289",
   :preview
   "(d/q '[:find [?order-id ...] :in $ ?customer :where [?order :order/customer ?customer] [?o",
   :fixture :reading-data},
  "07-reading-data#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:343",
   :preview "(d/pull db pattern eid-or-lookup-ref)"},
  "07-reading-data#clojure-006"
  {:status :runnable,
   :reason "Runs against the shared Chapter 7 pull fixture.",
   :source "07-reading-data.md:366",
   :preview
   "(d/pull db '[:user/name :user/email] [:user/email \"alice@example.com\"]) ;; => {:user/name ",
   :fixture :reading-data},
  "07-reading-data#clojure-007"
  {:status :runnable,
   :reason "Runs against the shared Chapter 7 pull fixture.",
   :source "07-reading-data.md:396",
   :preview
   "(d/pull db '[:user/name :user/email] 1) ;; => {:user/name \"Alice\", :user/email \"alice@exam",
   :fixture :reading-data},
  "07-reading-data#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:420",
   :preview "(:user/name (d/entity db 1)) ;; => \"Alice\""},
  "07-reading-data#clojure-009"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:444",
   :preview
   "(d/pull db '[*] [:user/email \"alice@example.com\"]) ;; => {:db/id 1, ;; :user/name \"Alice\","},
  "07-reading-data#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:483",
   :preview
   "(d/pull db '[:order/id {:order/customer [:user/name :user/email]}] [:order/id \"o-1001\"])"},
  "07-reading-data#clojure-011"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "07-reading-data.md:510",
   :preview
   "{:order/id \"o-1001\", :order/customer {:user/name \"Alice\", :user/email \"alice@example.com\"}"},
  "07-reading-data#clojure-012"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:521",
   :preview
   "(d/pull db '[:order/id :order/customer] [:order/id \"o-1001\"]) ;; => {:order/id \"o-1001\", :"},
  "07-reading-data#clojure-013"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:561",
   :preview
   "(d/pull-many db '[:user/name] [[:user/email \"alice@example.com\"] [:user/email \"bob@example"},
  "07-reading-data#clojure-014"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:597",
   :preview
   "(d/q '[:find [(pull ?user [:user/name :user/email]) ...] :where [?user :user/email]] db)"},
  "07-reading-data#clojure-015"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:632",
   :preview
   "(def public-user-pattern [:user/name :user/email]) (d/q '[:find [(pull ?user ?pattern) ..."},
  "07-reading-data#clojure-016"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:681",
   :preview
   "(d/q '[:find ?user (pull $users ?user [:user/name]) :in $users $orders :where [$users ?use"},
  "07-reading-data#clojure-017"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:732",
   :preview
   "(d/pull db '[:user/name {:order/_customer [:order/id]}] [:user/email \"alice@example.com\"])"},
  "07-reading-data#clojure-018"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:764",
   :preview
   "(d/pull db '[:user/name :order/_customer] [:user/email \"alice@example.com\"]) ;; => {:user/"},
  "07-reading-data#clojure-019"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:815",
   :preview
   "(d/pull db '[[:user/friends :limit 5]] [:user/email \"bob@example.com\"])"},
  "07-reading-data#clojure-020"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:842",
   :preview
   "(d/pull db '[:user/name {[:user/friends :limit 5] [:user/name]}] [:user/email \"bob@example"},
  "07-reading-data#clojure-021"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:874",
   :preview
   "(d/pull db '[:user/name [:user/active? :default true]] [:user/email \"alice@example.com\"]) "},
  "07-reading-data#clojure-022"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:906",
   :preview
   "(d/pull db '[[:user/name :as :name] [:user/email :as :email]] [:user/email \"alice@example."},
  "07-reading-data#clojure-023"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:944",
   :preview
   "(d/pull db '[[:user/name :as :name :xform clojure.string/upper-case]] [:user/email \"alice@"},
  "07-reading-data#clojure-024"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:981",
   :preview
   "(d/pull db '[[:user/nickname :xform vector]] [:user/email \"alice@example.com\"]) ;; => {:us"},
  "07-reading-data#clojure-025"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1012",
   :preview
   "(d/pull db '[[:user/nickname :default \"unknown\" :xform vector]] [:user/email \"alice@exampl"},
  "07-reading-data#clojure-026"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1058",
   :preview
   "(d/pull db '[:category/name {:category/children 2}] [:category/slug \"databases\"])"},
  "07-reading-data#clojure-027"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1088",
   :preview
   "(d/pull db '[:category/name {:category/children ...}] [:category/slug \"databases\"])"},
  "07-reading-data#clojure-028"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1119",
   :preview
   "(d/pull db '[:category/name {:category/_children 3}] [:category/slug \"lmdb\"])"},
  "07-reading-data#clojure-029"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1156",
   :preview
   "(d/pull db '[:order/id {:order/items [:line/sku :line/qty]}] [:order/id \"o-1001\"])"},
  "07-reading-data#clojure-030"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1194",
   :preview
   "(def alice (d/entity db [:user/email \"alice@example.com\"]))"},
  "07-reading-data#clojure-031"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1217",
   :preview
   "(:db/id alice) ;; => 1 (:user/name alice) ;; => \"Alice\" (alice :user/email) ;; => \"alice@e"},
  "07-reading-data#clojure-032"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1261",
   :preview
   "(when-let [alice (d/entity db [:user/email \"alice@example.com\"])] (if (:user/active? alice"},
  "07-reading-data#clojure-033"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1299",
   :preview
   "(pr-str (d/entity db [:user/email \"alice@example.com\"])) ;; => \"{:db/id 1}\" (d/touch (d/en"},
  "07-reading-data#clojure-034"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1332",
   :preview
   "(def alice-ent (d/touch (d/entity db [:user/email \"alice@example.com\"]))) ;; Do this when "},
  "07-reading-data#clojure-035"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1352",
   :preview
   "(def bob (d/entity db [:user/email \"bob@example.com\"])) (:user/friends bob) ;; => a set co"},
  "07-reading-data#clojure-036"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1396",
   :preview
   "(def alice (d/entity db [:user/email \"alice@example.com\"])) (map :order/id (:order/_custom"},
  "07-reading-data#clojure-037"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1442",
   :preview
   "(def db1 (d/db conn)) (def alice-v1 (d/entity db1 [:user/email \"alice@example.com\"])) (d/t"},
  "07-reading-data#clojure-038"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1516",
   :preview
   "(defn rename-user [db email new-name] (if-some [ent (d/entity db [:user/email email])] [[:"},
  "07-reading-data#clojure-039"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1544",
   :preview
   "(def db1 (d/db conn)) (def alice (d/entity db1 [:user/email \"alice@example.com\"])) (def st"},
  "07-reading-data#clojure-040"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1563",
   :preview
   "(d/transact! conn [staged-alice]) (d/pull (d/db conn) '[:user/name {:user/friends [:user/n"},
  "07-reading-data#clojure-041"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "07-reading-data.md:1577",
   :preview
   "(def db2 (d/db conn)) (def alice2 (d/entity db2 [:user/email \"alice@example.com\"])) (def b"},
  "08-datalog-fundamentals#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:36",
   :preview
   "(d/q '[:find ?e :where [?e :user/name \"Alice\"] [?e :user/city \"London\"]] db)"},
  "08-datalog-fundamentals#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:81",
   :preview
   "(d/q '[:find ?attr :where [?p :person/name] [?p ?attr]] db)"},
  "08-datalog-fundamentals#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:126",
   :preview
   "(d/q '[:find [?name ...] :where [_ :user/name ?name]] db)"},
  "08-datalog-fundamentals#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:137",
   :preview
   "(d/q '[:find ?edge :where [?edge :edge/from ?node] [?edge :edge/to ?node]] db)"},
  "08-datalog-fundamentals#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:147",
   :preview
   ";; Assumes :doc/body has :db/fulltext true and :db.fulltext/autoDomain true. (d/q '[:find "},
  "08-datalog-fundamentals#clojure-006"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "08-datalog-fundamentals.md:162",
   :preview "[?e :order/customer _] [?e :order/customer]"},
  "08-datalog-fundamentals#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:205",
   :preview
   "(d/q '[:find ?name ?total :where [?u :user/city \"London\"] [?u :user/name ?name] [?o :order"},
  "08-datalog-fundamentals#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:308",
   :preview
   ";; Returns: #{[\"Alice\" 30] [\"Bob\" 42]} (d/q '[:find ?name ?age :where [?e :user/name ?name"},
  "08-datalog-fundamentals#clojure-009"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:353",
   :preview
   "(d/q '[:find [?name ...] :where [?e :user/name ?name]] db)"},
  "08-datalog-fundamentals#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:381",
   :preview
   ";; One tuple (d/q '[:find [?name ?email] :in $ ?user-id :where [?user-id :user/name ?name]"},
  "08-datalog-fundamentals#clojure-011"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:452",
   :preview
   ";; Returns: #{{:name \"Alice\" :age 30} {:name \"Bob\" :age 42}} (d/q '[:find ?name ?age :keys"},
  "08-datalog-fundamentals#clojure-012"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:499",
   :preview
   ";; Find the average age of all users (d/q '[:find (avg ?age) . ; The dot returns a single "},
  "08-datalog-fundamentals#clojure-013"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:551",
   :preview
   "(d/q '[:find ?customer (sum ?subtotal) (sum ?tax) (+ (sum ?subtotal) (sum ?tax)) :where [?"},
  "08-datalog-fundamentals#clojure-014"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:604",
   :preview
   "(d/q '[:find [(pull ?e [:user/name {:user/orders [:order/id]}]) ...] :where [?e :user/city"},
  "08-datalog-fundamentals#clojure-015"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:659",
   :preview
   ";; Find users older than 30 (d/q '[:find ?name ?age :where [?e :user/name ?name] [?e :user"},
  "08-datalog-fundamentals#clojure-016"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:703",
   :preview
   ";; Compute an order total from subtotal and tax, and bind the result to ?total (d/q '[:fin"},
  "08-datalog-fundamentals#clojure-017"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:759",
   :preview
   ";; Example of a custom function that needs full qualification (ns my-app.queries (:require"},
  "08-datalog-fundamentals#clojure-018"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:795",
   :preview
   ";; Wrong for revenue: duplicate amount values collapse. (d/q '[:find (sum ?amount) . :wher"},
  "08-datalog-fundamentals#clojure-019"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:828",
   :preview
   ";; Correct: each order contributes its amount. (d/q '[:find (sum ?amount) . :with ?order :"},
  "08-datalog-fundamentals#clojure-020"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:872",
   :preview
   ";; Find customers whose total spend and largest order both pass thresholds. (d/q '[:find ?"},
  "08-datalog-fundamentals#clojure-021"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:927",
   :preview
   ";; Find users, ordered by age descending, then name ascending (d/q '[:find ?name ?age :whe"},
  "08-datalog-fundamentals#clojure-022"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:968",
   :preview
   ";; Sort customers by total spend descending, then customer name ascending. (d/q '[:find ?c"},
  "08-datalog-fundamentals#clojure-023"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:1018",
   :preview
   ";; Get the second page of users (limit 10, offset 10) (d/q '[:find ?name ?age :where [?e :"},
  "08-datalog-fundamentals#clojure-024"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "08-datalog-fundamentals.md:1065",
   :preview
   "[:find ?name ?age :where [?e :user/name ?name] [?e :user/age ?age] :order-by [?name :asc] "},
  "08-datalog-fundamentals#clojure-025"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:1077",
   :preview
   "(defn page-users [conn {:keys [offset limit] :or {offset 0 limit 50}}] (d/q {:find '[?name"},
  "08-datalog-fundamentals#clojure-026"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:1111",
   :preview
   "(d/q '[:find ?e :where (or [?e :user/status :pending] [?e :user/status :flagged])] db) (d/"},
  "08-datalog-fundamentals#clojure-027"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "08-datalog-fundamentals.md:1158",
   :preview
   "(or (and [?e :user/status :pending] [?e :user/priority :high]) (and [?e :user/status :flag"},
  "08-datalog-fundamentals#clojure-028"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:1177",
   :preview
   ";; ANTI-PATTERN: VERY SLOW! (d/q '[:find ?e :where [?e :user/city \"London\"] [(empty? (q (q"},
  "08-datalog-fundamentals#clojure-029"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:1230",
   :preview
   ";; Find users in London with no orders (d/q '[:find ?e :where [?e :user/city \"London\"] (no"},
  "08-datalog-fundamentals#clojure-030"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:1291",
   :preview
   ";; Join across two databases: a user db and an orders db (d/q '[:find ?name ?order-total :"},
  "08-datalog-fundamentals#clojure-031"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "08-datalog-fundamentals.md:1367",
   :preview
   ";; Query an in-memory collection of tuples (def my-data [[1 :user/name \"Alice\"] [1 :user/a"},
  "09-rules-recursion#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:58",
   :preview
   "(def user-rules '[[(is-active? ?e) [?e :user/status :active] [?e :user/verified? true]] [("},
  "09-rules-recursion#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:119",
   :preview
   "(let [rules '[[(is-active? ?e) [?e :user/status :active] [?e :user/verified? true]]]] (d/q"},
  "09-rules-recursion#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:206",
   :preview
   "(def user-status-rules '[[(active-user ?u) [?u :user/status :user.status/active] [?u :user"},
  "09-rules-recursion#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:276",
   :preview
   "(def access-rules '[[(has-access? ?user ?resource) [?user :user/role :admin] [?resource :r"},
  "09-rules-recursion#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:340",
   :preview
   "(def alias-rules '[[(alias-of ?person ?alias) [?person :person/name ?alias]] [(alias-of ?p"},
  "09-rules-recursion#clojure-006"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:414",
   :preview
   "(d/q '[:find ?alias :in $ ?person-id :where [?person :person/id ?person-id] (or-join [?per"},
  "09-rules-recursion#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:509",
   :preview
   "(def reasoning-rules '[;; Rule 1: High Spender [(is-high-spender? ?u) [?u :user/total-spen"},
  "09-rules-recursion#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:609",
   :preview
   "(d/transact! conn [{:user/id \"u1\" :user/total-spent 6000 :user/order-count 25} {:user/id \""},
  "09-rules-recursion#clojure-009"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:716",
   :preview
   "(def org-rules '[[(reports-to ?sub ?boss) [?sub :employee/manager ?boss]] ; Base case: dir"},
  "09-rules-recursion#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:788",
   :preview
   "(d/transact! conn [{:db/id -1 :employee/name \"Alice\"} {:db/id -2 :employee/name \"Bob\" :emp"},
  "09-rules-recursion#clojure-011"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "09-rules-recursion.md:893",
   :preview
   "[(reports-to ?sub ?boss) [?sub :employee/manager ?boss]]"},
  "09-rules-recursion#clojure-012"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "09-rules-recursion.md:909",
   :preview
   "[(reports-to ?sub ?boss) [?sub :employee/manager ?intermediate] (reports-to ?intermediate "},
  "09-rules-recursion#clojure-013"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:971",
   :preview
   "(def distance-rules '[[(distance-squared ?p1 ?p2 ?d2) [?p1 :point/x ?x1] [?p1 :point/y ?y1"},
  "09-rules-recursion#clojure-014"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:1038",
   :preview
   "(d/q '[:find ?name-a ?name-b :in $ % :where [?a :point/name ?name-a] [?b :point/name ?name"},
  "09-rules-recursion#clojure-015"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:1085",
   :preview
   "(d/q '[:find ?name ?dist2 :in $ % :where [?origin :point/name \"origin\"] [?p :point/name ?n"},
  "09-rules-recursion#clojure-016"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:1146",
   :preview
   "(def pricing-rules '[[(price-candidate ?user ?product ?price) [?product :product/base-pric"},
  "09-rules-recursion#clojure-017"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "09-rules-recursion.md:1265",
   :preview
   "(d/q '[:find ?product-id ?final-price :in $ % ?user-id :where [?user :user/id ?user-id] [?"},
  "10-key-value-store#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:47",
   :preview
   "(require '[datalevin.core :as d]) ;; Open the store (def kv (d/open-kv \"/tmp/my-kv-store\")"},
  "10-key-value-store#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:124",
   :preview
   ";; Open an in-memory KV store (no file persistence), directory can be nil (def mem-kv (d/o"},
  "10-key-value-store#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:175",
   :preview
   ";; Open a regular dbi called \"people\" in kv store (d/open-dbi kv \"people\")"},
  "10-key-value-store#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:205",
   :preview
   ";; Open a list-dbi (DUPSORT DBI) called \"tags\" (d/open-list-dbi kv \"tags\")"},
  "10-key-value-store#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:307",
   :preview
   "(def event-key-type [:string :instant]) (d/open-dbi kv \"events\") (d/transact-kv kv [[:put "},
  "10-key-value-store#clojure-006"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:426",
   :preview
   "(def score-type [:double :string]) (d/open-list-dbi kv \"scores-by-board\") (d/transact-kv k"},
  "10-key-value-store#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:531",
   :preview
   "[:put dbi-name key value] [:put dbi-name key value key-type value-type] [:put dbi-name key"},
  "10-key-value-store#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:560",
   :preview
   ";; Add multiple values to the same key in a list dbi (d/transact-kv kv [[:put \"tags\" \"cloj"},
  "10-key-value-store#clojure-009"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:598",
   :preview
   "(d/transact-kv kv \"people\" [[:put 1 \"Alice\"] [:put 2 \"Bob\"] [:del 3]] :long :string)"},
  "10-key-value-store#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:645",
   :preview
   "(d/transact-kv kv [[:put \"people\" 1 \"Alice\" :long :string] [:put \"people\" 2 \"Bob\" :long :s"},
  "10-key-value-store#clojure-011"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:693",
   :preview
   ";; Get all values for a key (d/get-list kv \"tags\" \"clojure\" :string :string) ;; => (\"expre"},
  "10-key-value-store#clojure-012"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:766",
   :preview
   "(def kv (d/open-kv \"/tmp/my-kv\" {:spill-opts {:spill-threshold 100}}))"},
  "10-key-value-store#clojure-013"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:797",
   :preview
   "(def conn (d/get-conn \"/tmp/my-db\" schema {:kv-opts {:spill-opts {:spill-threshold 100}}})"},
  "10-key-value-store#clojure-014"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:874",
   :preview
   "(def kv (d/open-kv nil {:inmemory? true})) (d/open-dbi kv \"events\") (def event-key [:strin"},
  "10-key-value-store#clojure-015"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:932",
   :preview
   "(d/visit-list kv \"scores-by-board\" (fn [value-buf] (println (d/read-buffer value-buf score"},
  "10-key-value-store#clojure-016"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:971",
   :preview
   "(d/list-range-filter kv \"scores-by-board\" (fn [kvp] (let [[score _user] (d/read-buffer (d/"},
  "10-key-value-store#clojure-017"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:1028",
   :preview
   "(import '[java.nio ByteBuffer]) (def score-type [:double :string]) (def cutoff (doto (Byte"},
  "10-key-value-store#clojure-018"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:1063",
   :preview
   "(d/visit-list kv \"scores-by-board\" (fn [[score user]] (println user score)) \"daily\" :strin"},
  "10-key-value-store#clojure-019"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:1130",
   :preview
   "(d/open-dbi kv \"people\") (d/transact-kv kv [[:put \"people\" 1 \"Alice\" :long :string] [:put "},
  "10-key-value-store#clojure-020"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:1211",
   :preview
   "(d/open-dbi kv \"counters\") (d/with-transaction-kv [tx kv] (let [current (or (d/get-value t"},
  "10-key-value-store#clojure-021"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "10-key-value-store.md:1289",
   :preview
   "(d/with-transaction-kv [tx kv {:timeout-ms 5000}] (d/transact-kv tx \"counters\" [[:put \"hit"},
  "11-schema-design#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "11-schema-design.md:38",
   :preview
   ";; Schema definition {:user/email {:db/valueType :db.type/string :db/unique :db.unique/ide"},
  "11-schema-design#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "11-schema-design.md:121",
   :preview
   "(def schema {:line-item/order-id {:db/valueType :db.type/string} :line-item/sku {:db/value"},
  "11-schema-design#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "11-schema-design.md:170",
   :preview
   ";; Create or update by component attributes. The tuple is maintained. (d/transact! conn [{"},
  "11-schema-design#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "11-schema-design.md:267",
   :preview
   ";; Build a tuple in the query and match it against the derived attribute. (d/q '[:find ?li"},
  "11-schema-design#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "11-schema-design.md:345",
   :preview
   ";; Schema includes a ref attribute for the enum-valued field (def schema {:order/id {:db/v"},
  "11-schema-design#clojure-006"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "11-schema-design.md:492",
   :preview
   ";; Instead of many references in one entity: {:user/id \"u-1\" :user/roles [:role/admin :rol"},
  "11-schema-design#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "11-schema-design.md:670",
   :preview
   ";; Schema includes a ref attribute for order status (def schema {:order/id {:db/valueType "},
  "11-schema-design#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "11-schema-design.md:769",
   :preview "(d/schema conn)"},
  "11-schema-design#clojure-009"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "11-schema-design.md:811",
   :preview
   ";; Add or refine schema for an open connection. (d/update-schema conn {:user/last-login {:"},
  "11-schema-design#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "11-schema-design.md:906",
   :preview
   "(def schema {:user/id {:db/valueType :db.type/string :db/unique :db.unique/identity} :user"},
  "11-schema-design#clojure-011"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "11-schema-design.md:1010",
   :preview
   "{:bank/routing-number {:db/valueType :db.type/string :db.attr/preds ['my.app.payments/rout"},
  "11-schema-design#clojure-012"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "11-schema-design.md:1029",
   :preview
   "(ns my.app.payments (:require [clojure.string :as str] [datalevin.core :as d])) (defn non-"},
  "12-relational-modeling#clojure-001"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "12-relational-modeling.md:72",
   :preview
   ";; Associative Entity: Membership {:membership/user 101 ; ref to User :membership/group 20"},
  "12-relational-modeling#clojure-002"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "12-relational-modeling.md:100",
   :preview
   "{:user/id {:db/valueType :db.type/string :db/unique :db.unique/identity} :product/sku {:db"},
  "12-relational-modeling#clojure-003"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "12-relational-modeling.md:177",
   :preview
   ";; A thin model: the relationship has nowhere to put its own facts. {:order/products [[:pr"},
  "12-relational-modeling#clojure-004"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "12-relational-modeling.md:211",
   :preview
   "{:account/id \"acct-1\" :account/type :account.type/business :account/name \"Acme Corp\"}"},
  "12-relational-modeling#clojure-005"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "12-relational-modeling.md:259",
   :preview
   "{:price/vendor {:db/valueType :db.type/string} :price/sku {:db/valueType :db.type/string} "},
  "12-relational-modeling#clojure-006"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "12-relational-modeling.md:314",
   :preview
   "(d/transact! conn [{:price/vendor \"acme\" :price/sku \"SKU-42\" :price/region \"us-west\" :pric"},
  "12-relational-modeling#clojure-007"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "12-relational-modeling.md:377",
   :preview
   "{:place/latlon {:db/valueType :db.type/tuple :db/tupleType :db.type/double} :reading/value"},
  "12-relational-modeling#clojure-008"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "12-relational-modeling.md:425",
   :preview
   "{:place/lat {:db/valueType :db.type/double} :place/lon {:db/valueType :db.type/double} :re"},
  "12-relational-modeling#clojure-009"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "12-relational-modeling.md:519",
   :preview
   "(def school-schema {:student/id {:db/valueType :db.type/string :db/unique :db.unique/ident"},
  "12-relational-modeling#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "12-relational-modeling.md:706",
   :preview
   "(d/transact! conn [{:student/id \"s-100\" :student/name \"Ada\"} {:course/code \"CS101\" :course"},
  "12-relational-modeling#clojure-011"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "12-relational-modeling.md:818",
   :preview
   "(let [enrollment (d/q '[:find ?enrollment . :in $ ?student-id ?course-code ?term-id ?secti"},
  "12-relational-modeling#clojure-012"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "12-relational-modeling.md:906",
   :preview
   "(d/q '[:find ?student-name ?course-title ?section ?instructor-name ?status :in $ ?term-id "},
  "12-relational-modeling#clojure-013"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "12-relational-modeling.md:1012",
   :preview
   "(def schema {:user/email {:db/valueType :db.type/string :db/unique :db.unique/identity :db"},
  "12-relational-modeling#clojure-014"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "12-relational-modeling.md:1118",
   :preview
   "{:order/id \"ord-1001\" :order/customer [:customer/id \"cust-42\"] :order/status :order.status"},
  "12-relational-modeling#clojure-015"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "12-relational-modeling.md:1127",
   :preview
   "[2001 :order/id \"ord-1001\"] [2001 :order/customer 1001] [2001 :order/status :order.status/"},
  "12-relational-modeling#clojure-016"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "12-relational-modeling.md:1155",
   :preview
   "[:find ?order-id ?email :where [?o :order/id ?order-id] [?o :order/status :order.status/pa"},
  "12-relational-modeling#clojure-017"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "12-relational-modeling.md:1169",
   :preview
   "[:find ?status (count ?o) :where [?o :order/status ?status]]"},
  "12-relational-modeling#clojure-018"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "12-relational-modeling.md:1222",
   :preview
   "(def ecommerce-schema {;; Noun: Product :product/sku {:db/unique :db.unique/identity :db/v"},
  "13-graph-modeling#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:75",
   :preview
   "(d/q '[:find ?suggestion :in $ ?user :where [?user :user/follows ?intermediate] [?intermed"},
  "13-graph-modeling#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:125",
   :preview
   "(def reachability-rules '[[(reachable ?start ?end) [?start :link/to ?end]] [(reachable ?st"},
  "13-graph-modeling#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:177",
   :preview
   "(def place-country-rules '[[(place-country ?place ?country) [?place :place/type \"Country\"]"},
  "13-graph-modeling#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:268",
   :preview
   "(def friends-3-rules '[[(friends-3 ?start ?friend ?dist) [?k :knows/person1 ?start] [?k :k"},
  "13-graph-modeling#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:421",
   :preview
   "(def alternating-rules '[[(path-a ?from ?to) [?from :edge/a ?to]] [(path-a ?from ?to) [?mi"},
  "13-graph-modeling#clojure-006"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:523",
   :preview
   "(d/q '[:find ?person :in $ ?person-id :where [?start :person/id ?person-id] (or-join [?sta"},
  "13-graph-modeling#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:610",
   :preview
   "(d/q '[:find ?person-id :in $ ?start-person-id :where [?start :person/id ?start-person-id]"},
  "13-graph-modeling#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:712",
   :preview
   "(def movie-schema {:person/name {:db/valueType :db.type/string :db/unique :db.unique/ident"},
  "13-graph-modeling#clojure-009"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:773",
   :preview
   "(d/q '[:find ?movie-title :in $ ?name-1 ?name-2 :where [?p1 :person/name ?name-1] [?p2 :pe"},
  "13-graph-modeling#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:850",
   :preview
   "(defn acted-with \"Return [other-person-eid movie-eid] pairs adjacent to actor.\" [db actor]"},
  "13-graph-modeling#clojure-011"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "13-graph-modeling.md:1064",
   :preview
   "{:component/name {:db/valueType :db.type/string :db/unique :db.unique/identity} :component"},
  "13-graph-modeling#clojure-012"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:1100",
   :preview
   "(def dependency-rules '[[(depends-on ?component ?dependency) [?component :component/uses ?"},
  "13-graph-modeling#clojure-013"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:1144",
   :preview
   "(d/q '[:find ?impacted-name :in $ % ?changed-name :where [?changed :component/name ?change"},
  "13-graph-modeling#clojure-014"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "13-graph-modeling.md:1203",
   :preview
   "{:route/from {:db/valueType :db.type/ref} :route/to {:db/valueType :db.type/ref} :route/km"},
  "13-graph-modeling#clojure-015"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:1241",
   :preview
   "(d/q '[:find ?mid-name ?km :in $ ?from-name ?to-name :where [?from :place/name ?from-name]"},
  "13-graph-modeling#clojure-016"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "13-graph-modeling.md:1330",
   :preview
   "{:person/name {:db/valueType :db.type/string} :person/advised {:db/valueType :db.type/ref "},
  "13-graph-modeling#clojure-017"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:1386",
   :preview
   "(def academic-rules '[[(author ?dissertation ?person) [?dissertation :dissertation/cid ?pe"},
  "13-graph-modeling#clojure-018"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:1452",
   :preview
   "(d/q '[:find [?ancestor-name ...] :in $ % ?student-name :where [?student :person/name ?stu"},
  "13-graph-modeling#clojure-019"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:1510",
   :preview
   "(def university-type-rules '[[(is-a ?x ?class) [?x :type :GraduateStudent] [(ground :Stude"},
  "13-graph-modeling#clojure-020"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:1629",
   :preview
   "(d/q '[:find ?student ?advisor :in $ % :where (is-a ?student ?class) [(= ?class :Student)]"},
  "13-graph-modeling#clojure-021"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:1681",
   :preview
   "(def same-generation-rules '[[(same-generation ?x ?y) [?x :parent _] [(ground ?x) ?y]] [(s"},
  "13-graph-modeling#clojure-022"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:1751",
   :preview
   "(def root-post-rule '[;; base case [(root-post ?m ?post) [?m :message/containerOf _] [(gro"},
  "13-graph-modeling#clojure-023"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "13-graph-modeling.md:1807",
   :preview
   "(d/q '[:find ?forum-id ?forum-title ?moderator-id ?moderator-first-name ?moderator-last-na"},
  "14-document-modeling#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:38",
   :preview
   ";; Schema: :post/comments owns comment entities. {:post/comments {:db/valueType :db.type/r"},
  "14-document-modeling#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:96",
   :preview
   "(d/transact! conn [{:db/id 200 :user/name \"Ada\" :user/email \"ada@example.com\"} {:db/id 1 :"},
  "14-document-modeling#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:160",
   :preview
   "(d/pull db [:post/title {:post/comments [:comment/body {:comment/author [:user/name]}]}] 1"},
  "14-document-modeling#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:205",
   :preview
   "(defn leaf-paths \"Returns paths to scalar leaves in a nested map/vector.\" ([root] {:pre [("},
  "14-document-modeling#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:360",
   :preview
   "(def order-doc {:order/id \"o-1001\" :customer {:email \"ada@example.com\" :name \"Ada\"} :lines"},
  "14-document-modeling#clojure-006"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:443",
   :preview
   "(d/q '[:find ?sku ?qty :where [:lines ?i :sku ?sku] [:lines ?i :qty ?qty]] tuples) ;; => #"},
  "14-document-modeling#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:490",
   :preview
   "(def tuples-with-paths (nested->tuples order-doc {:paths? true})) (d/q '[:find ?path ?sku "},
  "14-document-modeling#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:567",
   :preview
   "(def schema {:user/metadata {:db/valueType :db.type/idoc :db/domain \"user_metadata\"} :user"},
  "14-document-modeling#clojure-009"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:667",
   :preview
   "(d/q '[:find ?e ?a :where [(idoc-match $ {:status \"active\"} {:domains [\"profiles\"]}) [[?e "},
  "14-document-modeling#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:715",
   :preview
   "(d/transact! conn [{:db/id 101 :user/metadata {:theme \"dark\" :profile {:age 30 :name \"Alic"},
  "14-document-modeling#clojure-011"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:781",
   :preview
   "(d/transact! conn [{:db/id 201 :doc/title \"Search Guide\" :doc/markdown \"# Guide ## Install"},
  "14-document-modeling#clojure-012"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "14-document-modeling.md:852",
   :preview
   "{:guide {:install \"Run clj -M:dev.\" :configure-search \"Set :index-position? true for phras"},
  "14-document-modeling#clojure-013"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:892",
   :preview
   "(d/q '[:find ?title ?install :where [?e :doc/title ?title] [(idoc-match $ :doc/markdown {:"},
  "14-document-modeling#clojure-014"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:940",
   :preview
   "(d/q '[:find ?e :where [(idoc-match $ :doc/markdown {\"Guide\" {\"Configure Search\" \"Set :ind"},
  "14-document-modeling#clojure-015"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:992",
   :preview
   "(d/transact! conn [[:db.fn/patchIdoc 101 :user/metadata [[:set [:profile :display-name] \"A"},
  "14-document-modeling#clojure-016"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:1047",
   :preview
   ";; Find users with a dark theme in their metadata idoc. (d/q '[:find ?e :where [(idoc-matc"},
  "14-document-modeling#clojure-017"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:1075",
   :preview
   "(d/q '[:find ?e :where [(idoc-match $ :user/metadata {:profile {:age 30 :name \"Alice\"}}) ["},
  "14-document-modeling#clojure-018"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:1107",
   :preview
   "(d/q '[:find ?e :where [(idoc-match $ :user/metadata {:tags \"admin\"}) [[?e _ _]]]] db)"},
  "14-document-modeling#clojure-019"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:1136",
   :preview
   "(d/q '[:find ?e :where [(idoc-match $ :user/metadata {:profile [:or {:age 30} {:age 40}]})"},
  "14-document-modeling#clojure-020"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:1174",
   :preview
   ";; Inline predicate. (d/q '[:find ?e :where [(idoc-match $ :user/metadata {:profile {:age "},
  "14-document-modeling#clojure-021"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:1263",
   :preview
   ";; Match any single key under :profile with value >= 30. (d/q '[:find ?e :in $ ?q :where ["},
  "14-document-modeling#clojure-022"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:1328",
   :preview
   "(d/q '[:find ?e :where [(idoc-match $ :user/raw-json {\"middle\" (nil?)}) [[?e _ _]]]] db)"},
  "14-document-modeling#clojure-023"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "14-document-modeling.md:1363",
   :preview
   "(d/q '[:find ?e ?email :where [(idoc-match $ :user/metadata {:theme \"dark\"}) [[?e _ ?doc]]"},
  "15-index-architecture#clojure-001"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "15-index-architecture.md:31",
   :preview "[101 :user/email \"alice@example.com\"]"},
  "15-index-architecture#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "15-index-architecture.md:56",
   :preview
   "(first (d/datoms db :ave :user/email \"alice@example.com\")) ;=> #datalevin/Datom [101 :user"},
  "15-index-architecture#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "15-index-architecture.md:63",
   :preview
   "(let [report (d/transact! conn [{:user/email \"alice@example.com\"}]) datom (first (:tx-data"},
  "15-index-architecture#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "15-index-architecture.md:264",
   :preview
   ";; All datoms for entity 101, ordered by attribute and value. (d/datoms db :eav 101) ;; A "},
  "15-index-architecture#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "15-index-architecture.md:319",
   :preview
   ";; All attributes and values for entity 101. (d/search-datoms db 101 nil nil) ;; All entit"},
  "15-index-architecture#clojure-006"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "15-index-architecture.md:376",
   :preview
   ";; How many datoms are attached to entity 101? (d/count-datoms db 101 nil nil) ;; How many"},
  "15-index-architecture#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "15-index-architecture.md:429",
   :preview
   ";; How many distinct ages exist? (d/cardinality db :user/age) ;; How many datoms use :user"},
  "15-index-architecture#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "15-index-architecture.md:492",
   :preview "(d/analyze db :user/age) (d/analyze db)"},
  "15-index-architecture#clojure-009"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "15-index-architecture.md:508",
   :preview
   ";; Start a forward scan at :user/age 30 in AVE order. (d/seek-datoms db :ave :user/age 30 "},
  "15-index-architecture#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "15-index-architecture.md:566",
   :preview
   "(d/seek-datoms db index c1 c2 c3 n) (d/rseek-datoms db index c1 c2 c3 n)"},
  "15-index-architecture#clojure-011"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "15-index-architecture.md:579",
   :preview
   ";; Ten datoms starting at [:user/age 30] in AVE order. (d/seek-datoms db :ave :user/age 30"},
  "15-index-architecture#clojure-012"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "15-index-architecture.md:591",
   :preview
   ";; Find users with age between 20 and 30, inclusive. (d/index-range db :user/age 20 30) ;;"},
  "15-index-architecture#clojure-013"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "15-index-architecture.md:636",
   :preview
   "(for [datom (d/datoms db :ave :user/age 30)] {:entity (d/datom-e datom) :attr (d/datom-a d"},
  "16-full-text-search#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:62",
   :preview
   "(def schema {:post/title {:db/valueType :db.type/string :db/fulltext true :db.fulltext/aut"},
  "16-full-text-search#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:223",
   :preview
   "(require '[datalevin.search-utils :as su]) (def stem-analyzer (su/create-analyzer {:tokeni"},
  "16-full-text-search#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:240",
   :preview
   "(def conn (d/create-conn \"/tmp/posts\" {:post/body {:db/valueType :db.type/string :db/fullt"},
  "16-full-text-search#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:266",
   :preview
   "(def prefix-index-analyzer (su/create-analyzer {:tokenizer (su/create-regexp-tokenizer #\"\\"},
  "16-full-text-search#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:298",
   :preview
   "(def trigram-analyzer (su/create-analyzer {:tokenizer (su/create-regexp-tokenizer #\"\\W+\") "},
  "16-full-text-search#clojure-006"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:542",
   :preview
   "(d/q '[:find ?e ?a ?v :in $ ?q :where [(fulltext $ ?q) [[?e ?a ?v]]]] db \"red fox\")"},
  "16-full-text-search#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:579",
   :preview
   "(d/fulltext-datoms db \"red fox\" {:limit 10 :offset 20}) ;=> (#datalevin/Datom [123 :post/b"},
  "16-full-text-search#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:611",
   :preview
   "(d/q '[:find ?e ?a ?v :in $ ?q :where [(fulltext $ :post/title ?q) [[?e ?a ?v]]]] db \"cloj"},
  "16-full-text-search#clojure-009"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:648",
   :preview
   "(d/q '[:find ?e ?a ?v :in $ ?q :where [(fulltext $ ?q {:top 5}) [[?e ?a ?v]]]] db \"databas"},
  "16-full-text-search#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:708",
   :preview
   "(d/q '[:find ?e ?a ?v :in $ ?q :where [(fulltext $ ?q {:limit 20 :offset 40}) [[?e ?a ?v]]"},
  "16-full-text-search#clojure-011"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:769",
   :preview
   "(d/create-conn \"/tmp/search-db\" schema {:search-opts {:include-text? true}}) (d/create-con"},
  "16-full-text-search#clojure-012"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:839",
   :preview
   "(d/q '[:find ?e ?score :in $ ?q :where [(fulltext $ ?q {:top 5 :display :refs+scores}) [[?"},
  "16-full-text-search#clojure-013"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "16-full-text-search.md:892",
   :preview
   "[:and \"clojure\" \"database\"] ; both terms [:or \"fox\" \"red\"] ; either term [:not \"java\"] ; e"},
  "16-full-text-search#clojure-014"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:936",
   :preview
   "(d/q '[:find ?e ?a ?v :where [(fulltext $ [:and \"clojure\" [:not \"java\"]]) [[?e ?a ?v]]]] d"},
  "16-full-text-search#clojure-015"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "16-full-text-search.md:982",
   :preview "[:and {:phrase \"little lamb\"} \"fleece\"]"},
  "16-full-text-search#clojure-016"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1008",
   :preview
   "(d/create-conn \"/tmp/search-phrases\" schema {:search-domains {\"public\" {:index-position? t"},
  "16-full-text-search#clojure-017"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1053",
   :preview
   "(def lmdb (d/open-kv \"/tmp/search-phrases\")) (d/new-search-engine lmdb {:index-position? t"},
  "16-full-text-search#clojure-018"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1094",
   :preview
   "(d/q '[:find ?e ?a ?v :where [(fulltext $ {:phrase \"little lamb\"} {:domains [\"public\"] :to"},
  "16-full-text-search#clojure-019"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "16-full-text-search.md:1139",
   :preview
   "[:or \"fox\" {:phrase \"red fox\"} [:and \"black\" \"sheep\" [:not \"yellow\"]]]"},
  "16-full-text-search#clojure-020"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1216",
   :preview
   "(def search-schema {:post/title {:db/valueType :db.type/string :db/fulltext true :db.fullt"},
  "16-full-text-search#clojure-021"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1320",
   :preview
   "(def conn (d/create-conn \"/tmp/search-domains\" search-schema {:search-opts {:index-positio"},
  "16-full-text-search#clojure-022"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1403",
   :preview
   ";; Search every full-text domain. (d/q '[:find ?e ?a ?v :where [(fulltext $ \"clojure\") [[?"},
  "16-full-text-search#clojure-023"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1523",
   :preview
   "(def search-schema {:search/key {:db/valueType :db.type/string :db/unique :db.unique/ident"},
  "16-full-text-search#clojure-024"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1550",
   :preview
   "(d/transact! conn [{:search/key \"08-query#rules\" :search/type :section :search/doc \"08-que"},
  "16-full-text-search#clojure-025"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1573",
   :preview
   "(d/q '[:find (pull ?hit [:search/type :search/doc :search/anchor :search/title]) :in $ ?q "},
  "16-full-text-search#clojure-026"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1601",
   :preview
   "(def lmdb (d/open-kv \"/tmp/search-db\")) (def engine (d/new-search-engine lmdb {:index-posi"},
  "16-full-text-search#clojure-027"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1699",
   :preview
   "(def engine (d/new-search-engine lmdb {:include-text? true :index-position? true}))"},
  "16-full-text-search#clojure-028"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1746",
   :preview
   "(d/doc-indexed? engine 1) ;=> true (d/doc-count engine) ;=> 2 (d/remove-doc engine 2) (d/c"},
  "16-full-text-search#clojure-029"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1802",
   :preview "(d/add-doc engine doc-ref doc-text false)"},
  "16-full-text-search#clojure-030"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1830",
   :preview
   "(def lmdb (d/open-kv \"/tmp/search-db\")) (def writer (d/search-index-writer lmdb {:domain \""},
  "16-full-text-search#clojure-031"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:1940",
   :preview
   "(def conn (d/create-conn \"/tmp/search-db\" {:post/content {:db/valueType :db.type/string :d"},
  "16-full-text-search#clojure-032"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "16-full-text-search.md:2000",
   :preview
   "(def conn (d/create-conn \"/tmp/search-db\" {:post/content {:db/valueType :db.type/string :d"},
  "17-vector-search#clojure-001"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "17-vector-search.md:47",
   :preview
   "(def conn (d/create-conn \"/tmp/mydb\" {:id {:db/valueType :db.type/string :db/unique :db.un"},
  "17-vector-search#clojure-002"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "17-vector-search.md:200",
   :preview
   "(def embedding-schema {:doc/id {:db/valueType :db.type/string :db/unique :db.unique/identi"},
  "17-vector-search#clojure-003"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "17-vector-search.md:303",
   :preview
   "{:embedding-opts {:provider :openai-compatible :model \"text-embedding-3-small\" :base-url \""},
  "17-vector-search#clojure-004"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "17-vector-search.md:368",
   :preview
   "(def provider (d/new-embedding-provider {:provider :openai-compatible :model \"text-embeddi"},
  "17-vector-search#clojure-005"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "17-vector-search.md:467",
   :preview
   "(d/q '[:find ?i ?v :in $ ?q :where [(vec-neighbors $ :embedding ?q {:top 4}) [[?e ?a ?v]]]"},
  "17-vector-search#clojure-006"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "17-vector-search.md:538",
   :preview
   "(d/q '[:find ?e ?dist :in $ ?q :where [(vec-neighbors $ :embedding ?q {:top 4 :display :re"},
  "17-vector-search#clojure-007"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "17-vector-search.md:585",
   :preview
   "(d/q '[:find ?e ?dist :in $ ?q :where [(vec-neighbors $ ?q {:top 4 :domains [\"embedding\"] "},
  "17-vector-search#clojure-008"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "17-vector-search.md:644",
   :preview
   "(d/q '[:find ?id ?text :in $ ?q :where [(embedding-neighbors $ ?q {:domains [\"docs\"] :top "},
  "17-vector-search#clojure-009"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "17-vector-search.md:697",
   :preview
   "(d/q '[:find ?id ?dist :in $ ?q :where [(embedding-neighbors $ :doc/text ?q {:top 5 :displ"},
  "17-vector-search#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "17-vector-search.md:758",
   :preview
   "(def lmdb (d/open-kv \"/tmp/vector-db\")) (def index (d/new-vector-index lmdb {:dimensions 3"},
  "17-vector-search#clojure-011"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "17-vector-search.md:830",
   :preview
   "(d/vector-index-info index) ;=> {:size 2, :dimensions 300, :metric-type :euclidean, ...} ("},
  "17-vector-search#clojure-012"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "17-vector-search.md:971",
   :preview
   "(d/q '[:find ?title ?v :in $ ?target-vec :where [(vec-neighbors $ :product/embedding ?targ"},
  "17-vector-search#clojure-013"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "17-vector-search.md:1039",
   :preview
   "{:vector-opts {:dimensions 300 :metric-type :cosine :indexing-mode :async}}"},
  "17-vector-search#clojure-014"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "17-vector-search.md:1081",
   :preview
   "{:embedding-opts {:provider :openai-compatible :model \"text-embedding-3-small\" :api-key-en"},
  "18-hybrid-queries#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "18-hybrid-queries.md:136",
   :preview
   "(defn rrf-score [rank] (/ 1.0 (+ 60 rank))) (defn ranked [source hits] (map-indexed (fn [i"},
  "18-hybrid-queries#clojure-002"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "18-hybrid-queries.md:252",
   :preview
   "(def fusion-rules '[[(evidence ?e ?weight ?source ?query ?query-vec ?query-tags) [(fulltex"},
  "18-hybrid-queries#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "18-hybrid-queries.md:482",
   :preview
   "(def blacklist-fn {:udf/lang :app :udf/kind :query-fn :udf/id :kv/check-blacklist}) (d/q '"},
  "18-hybrid-queries#clojure-004"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "18-hybrid-queries.md:608",
   :preview
   "(d/q '[:find ?title ?dist :in $ ?search-term ?target-vec ?category :where ;; full-text sea"},
  "19-storage-tuning-durability#clojure-001"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "19-storage-tuning-durability.md:142",
   :preview
   "(def conn (d/get-conn \"/data/app-db\" schema {:wal? true})) (d/transact! conn [{:db/id -1 :"},
  "19-storage-tuning-durability#clojure-002"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "19-storage-tuning-durability.md:244",
   :preview
   "(def conn (d/get-conn \"/data/app-db\" schema {:wal? true :wal-durability-profile :strict}))"},
  "19-storage-tuning-durability#clojure-003"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "19-storage-tuning-durability.md:319",
   :preview
   "(def kv (d/open-kv \"/data/app-kv\" {:wal? true :snapshot-scheduler? true})) (d/txlog-waterm"},
  "19-storage-tuning-durability#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "19-storage-tuning-durability.md:392",
   :preview
   ";; Reclaim disk space (d/gc-txlog-segments! kv) ;; Keep WAL records from LSN 5000 onward ("},
  "19-storage-tuning-durability#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "19-storage-tuning-durability.md:428",
   :preview
   ";; Copy a live Datalog database from a connection (d/copy (d/db conn) \"/path/to/backup-db\""},
  "19-storage-tuning-durability#clojure-006"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "19-storage-tuning-durability.md:505",
   :preview
   ";; Copy and compact a Datalog database (d/copy (d/db conn) \"/path/to/compact-backup-db\" tr"},
  "19-storage-tuning-durability#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "19-storage-tuning-durability.md:586",
   :preview
   ";; Re-index a Datalog connection with existing schema/options (def reindexed-conn (d/re-in"},
  "19-storage-tuning-durability#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "19-storage-tuning-durability.md:699",
   :preview
   ";; Set mapsize to 128 GiB (in MiB) (d/get-conn path schema {:kv-opts {:mapsize 131072}})"},
  "19-storage-tuning-durability#clojure-009"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "19-storage-tuning-durability.md:842",
   :preview "(d/get-conn path schema {:kv-opts {:max-readers 512}})"},
  "20-batching-ingestion#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "20-batching-ingestion.md:32",
   :preview
   ";; Fire multiple async transactions (def f1 (d/transact-async conn batch-1)) (def f2 (d/tr"},
  "20-batching-ingestion#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "20-batching-ingestion.md:85",
   :preview
   ";; Async writes for throughput (d/transact-async conn batch-1) (d/transact-async conn batc"},
  "20-batching-ingestion#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "20-batching-ingestion.md:160",
   :preview
   "(require '[datalevin.core :as d]) (require '[datalevin.constants :as c]) ;; Raw KV store f"},
  "20-batching-ingestion#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "20-batching-ingestion.md:255",
   :preview
   ";; Sort data by entity id (let [sorted-datoms (sort-by :db/id raw-data)] (doseq [batch (pa"},
  "20-batching-ingestion#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "20-batching-ingestion.md:298",
   :preview
   "(let [db (d/db conn) previous-limit (d/datalog-index-cache-limit db)] (try (d/datalog-inde"},
  "20-batching-ingestion#clojure-006"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "20-batching-ingestion.md:384",
   :preview
   "(def schema {:user/id {:db/valueType :db.type/long :db/unique :db.unique/identity} :user/n"},
  "20-batching-ingestion#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "20-batching-ingestion.md:653",
   :preview
   "(def db (-> (d/empty-db \"/tmp/import-db\" schema {:closed-schema? true}) (d/fill-db (mapcat"},
  "21-query-optimization-profiling#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "21-query-optimization-profiling.md:98",
   :preview
   "(d/q '[:find ?sku ?qty :in $ ?order-id :where [?order :order/id ?order-id] [?line :line-it"},
  "21-query-optimization-profiling#clojure-002"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "21-query-optimization-profiling.md:242",
   :preview
   "[(> ?age 21)] ;; Becomes a range scan on :user/age >= 21"},
  "21-query-optimization-profiling#clojure-003"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "21-query-optimization-profiling.md:405",
   :preview
   "[(ancestor ?x ?y) [?x :parent ?y]] [(ancestor ?x ?y) [?x :parent ?z] (ancestor ?z ?y)]"},
  "21-query-optimization-profiling#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "21-query-optimization-profiling.md:446",
   :preview
   "(def graph-edb '[[a :link/to b] [b :link/to c] [b :link/to d] [d :link/to e] [p :link/to q"},
  "21-query-optimization-profiling#clojure-005"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "21-query-optimization-profiling.md:467",
   :preview
   "[(reachable ?start ?end) [?start :link/to ?end]] [(reachable ?start ?end) [?start :link/to"},
  "21-query-optimization-profiling#clojure-006"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "21-query-optimization-profiling.md:503",
   :preview "[:find ?end :where (reachable a ?end)]"},
  "21-query-optimization-profiling#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "21-query-optimization-profiling.md:589",
   :preview
   "(def query '[:find ?name :where [?e :user/name ?name] [?e :user/age ?age] [(> ?age 30)]]) "},
  "21-query-optimization-profiling#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "21-query-optimization-profiling.md:690",
   :preview "(d/explain {:run? true} query (d/db conn))"},
  "22-production-operations#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "22-production-operations.md:84",
   :preview
   "(ns my-app.query-test (:require [clojure.test :refer [deftest is]] [datalevin.core :as d])"},
  "22-production-operations#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "22-production-operations.md:195",
   :preview
   "(deftest upsert-updates-existing-user (let [schema {:user/email {:db/valueType :db.type/st"},
  "22-production-operations#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "22-production-operations.md:308",
   :preview
   "(def ^:dynamic *conn* nil) (defn with-fresh-db [f] (let [conn (d/create-conn nil test-sche"},
  "22-production-operations#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "22-production-operations.md:330",
   :preview
   "(ns my-app.persistence-test (:require [clojure.test :refer [deftest is]] [datalevin.core :"},
  "22-production-operations#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "22-production-operations.md:363",
   :preview
   "(d/create-conn nil schema {:kv-opts {:inmemory? true} :validate-data? true :closed-schema?"},
  "22-production-operations#clojure-006"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "22-production-operations.md:418",
   :preview
   "(deftest signup-adds-email-datom (let [conn (d/create-conn nil schema {:kv-opts {:inmemory"},
  "22-production-operations#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "22-production-operations.md:518",
   :preview
   "(let [conn (d/create-conn nil {:doc/content {:db/valueType :db.type/string :db/fulltext tr"},
  "22-production-operations#clojure-008"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "22-production-operations.md:756",
   :preview
   "(c/create-user client \"alice\" \"password123\") (c/create-role client :app.role/analyst) (c/g"},
  "22-production-operations#clojure-009"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "22-production-operations.md:842",
   :preview
   "(require '[datalevin.core :as d] '[datalevin.client :as cl]) (def schema {:name {:db/value"},
  "22-production-operations#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "22-production-operations.md:952",
   :preview
   "(d/q '[:find [?name ...] :where [?e :name ?name]] (d/db replica-conn)) ;; Throws: Replica "},
  "22-production-operations#clojure-011"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "22-production-operations.md:994",
   :preview
   "(require '[datalevin.client :as cl]) (def client (cl/new-client \"dtlv://replica-admin:pass"},
  "22-production-operations#clojure-012"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "22-production-operations.md:1063",
   :preview
   "(def follower (d/get-conn \"dtlv://user:pass@replica-host:8898/app\"))"},
  "22-production-operations#clojure-013"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "22-production-operations.md:1098",
   :preview
   "(require '[datalevin.client :as cl]) (def client (cl/new-client \"dtlv://admin:pass@node-a:"},
  "22-production-operations#clojure-014"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "22-production-operations.md:1263",
   :preview
   "(def kv (d/open-kv \"/data/companydb\")) (d/stat kv) (d/open-dbi kv \"datalevin/eav\") (d/stat"},
  "22-production-operations#clojure-015"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "22-production-operations.md:1346",
   :preview "(d/q '[:find ?e . :where [?e _ _]] (d/db conn))"},
  "22-production-operations#clojure-016"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "22-production-operations.md:1465",
   :preview
   "(defn increment-counter! [conn counter-id] (d/with-transaction [tx conn] (let [db @tx n (o"},
  "22-production-operations#clojure-017"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "22-production-operations.md:1540",
   :preview
   "(defn cas-conflict? [e] (= :transact/cas (:error (ex-data e)))) (defn retry-cas [f] (loop "},
  "23-agent-memory#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "23-agent-memory.md:80",
   :preview
   "(require '[datalevin.core :as d]) (def memory-schema {:user/id {:db/valueType :db.type/str"},
  "23-agent-memory#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "23-agent-memory.md:415",
   :preview
   ";; An \"Experience\" entity in Datalevin {:experience/id #uuid \"00000000-0000-0000-0000-0000"},
  "23-agent-memory#clojure-003"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "23-agent-memory.md:523",
   :preview
   "{:goal/id #uuid \"00000000-0000-0000-0000-000000000101\" :goal/status :goal.status/active :g"},
  "24-episodic-semantic-working-memory#clojure-001"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "24-episodic-semantic-working-memory.md:46",
   :preview
   "{:episode/id #uuid \"00000000-0000-0000-0000-000000000301\" :episode/timestamp #inst \"2024-0"},
  "24-episodic-semantic-working-memory#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "24-episodic-semantic-working-memory.md:129",
   :preview
   ";; Find all chat episodes from the last 24 hours. (d/q '[:find ?summary :in $ ?since :wher"},
  "24-episodic-semantic-working-memory#clojure-003"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "24-episodic-semantic-working-memory.md:197",
   :preview
   "{:episode/id #uuid \"00000000-0000-0000-0000-000000000302\" :episode/summary \"Release discus"},
  "24-episodic-semantic-working-memory#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "24-episodic-semantic-working-memory.md:282",
   :preview
   ";; Defining a concept. {:db/ident :concept/clojure :concept/description \"A modern, dynamic"},
  "24-episodic-semantic-working-memory#clojure-005"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "24-episodic-semantic-working-memory.md:331",
   :preview
   "{:fact/id #uuid \"00000000-0000-0000-0000-000000000303\" :fact/subject [:user/id \"alice\"] :f"},
  "24-episodic-semantic-working-memory#clojure-006"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "24-episodic-semantic-working-memory.md:427",
   :preview
   "{:wm/id #uuid \"00000000-0000-0000-0000-000000000104\" :wm/session [:session/id #uuid \"00000"},
  "24-episodic-semantic-working-memory#clojure-007"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "24-episodic-semantic-working-memory.md:560",
   :preview
   "{:local.doc/id #uuid \"00000000-0000-0000-0000-000000000304\" :local.doc/session [:session/i"},
  "24-episodic-semantic-working-memory#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "24-episodic-semantic-working-memory.md:714",
   :preview
   "(defn record-episode-and-candidate-fact! [conn {:keys [user-id session-id summary fact-kin"},
  "24-episodic-semantic-working-memory#clojure-009"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "24-episodic-semantic-working-memory.md:914",
   :preview
   "[{:episode/id #uuid \"00000000-0000-0000-0000-000000000311\" :episode/timestamp #inst \"2026-"},
  "24-episodic-semantic-working-memory#clojure-010"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "24-episodic-semantic-working-memory.md:1035",
   :preview
   "[{:fact/id #uuid \"00000000-0000-0000-0000-000000000321\" :fact/subject [:migration/id \"M184"},
  "24-episodic-semantic-working-memory#clojure-011"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "24-episodic-semantic-working-memory.md:1212",
   :preview
   "[{:component/id :component/write-path :component/name \"Write path\"} {:migration/id \"M184\" "},
  "24-episodic-semantic-working-memory#clojure-012"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "24-episodic-semantic-working-memory.md:1291",
   :preview
   "(d/q '[:find ?migration ?component ?check :where [?m :migration/id ?migration] [?m :migrat"},
  "25-recall-context-assembly#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "25-recall-context-assembly.md:90",
   :preview
   "(d/q '[:find ?fact :in $ % ?topic ?user ?since :where (or ;; Lens 1: recent episodic conte"},
  "25-recall-context-assembly#clojure-002"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "25-recall-context-assembly.md:172",
   :preview
   "(def text-hits (d/q '[:find ?e ?content ?score :in $ ?q-text ?user-id :where [(fulltext $ "},
  "25-recall-context-assembly#clojure-003"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "25-recall-context-assembly.md:407",
   :preview
   "(d/q '[:find ?content ?dist :in $ ?q :where [(embedding-neighbors $ :doc/content ?q {:top "},
  "25-recall-context-assembly#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "25-recall-context-assembly.md:483",
   :preview
   "(defn materialize-working-memory! [conn session-id selected-facts] (let [wm-id (random-uui"},
  "25-recall-context-assembly#clojure-005"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "25-recall-context-assembly.md:651",
   :preview
   "{:retrieval.run/id #uuid \"00000000-0000-0000-0000-000000000401\" :retrieval.run/session [:s"},
  "25-recall-context-assembly#clojure-006"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "25-recall-context-assembly.md:764",
   :preview
   "{:retrieval.scope/id #uuid \"00000000-0000-0000-0000-000000000403\" :retrieval.scope/session"},
  "25-recall-context-assembly#clojure-007"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "25-recall-context-assembly.md:848",
   :preview
   "{:goal {:id #uuid \"00000000-0000-0000-0000-000000000101\" :objective \"Monitor release readi"},
  "26-apperception-truth-maintenance#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "26-apperception-truth-maintenance.md:69",
   :preview
   ";; Query to check if a new fact contradicts existing knowledge. (d/q '[:find ?contradictio"},
  "26-apperception-truth-maintenance#clojure-002"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "26-apperception-truth-maintenance.md:141",
   :preview
   "{:episode/id #uuid \"00000000-0000-0000-0000-000000000701\" :episode/summary \"Alice moved th"},
  "26-apperception-truth-maintenance#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "26-apperception-truth-maintenance.md:261",
   :preview
   "(defn current-fact-ids [db user-id kind] (d/q '[:find [?fact-id ...] :in $ ?user-id ?kind "},
  "26-apperception-truth-maintenance#clojure-004"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "26-apperception-truth-maintenance.md:561",
   :preview
   "{:fact/id #uuid \"00000000-0000-0000-0000-000000000201\" :fact/content \"Alice prefers releas"},
  "26-apperception-truth-maintenance#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "26-apperception-truth-maintenance.md:647",
   :preview
   "(d/q '[:find ?fact ?confidence ?utility :in $ ?min-confidence ?min-utility :where [?f :fac"},
  "26-apperception-truth-maintenance#clojure-006"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "26-apperception-truth-maintenance.md:729",
   :preview
   "[{:episode/id #uuid \"00000000-0000-0000-0000-000000000801\" :episode/user [:user/id \"alice\""},
  "26-apperception-truth-maintenance#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "26-apperception-truth-maintenance.md:860",
   :preview
   "(d/q '[:find ?user (count ?episode) :keys user question-count :in $ ?topic ?since :where ["},
  "26-apperception-truth-maintenance#clojure-008"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "26-apperception-truth-maintenance.md:922",
   :preview
   "{:insight/id #uuid \"00000000-0000-0000-0000-000000000811\" :insight/kind :insight.kind/acti"},
  "26-apperception-truth-maintenance#clojure-009"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "26-apperception-truth-maintenance.md:1015",
   :preview
   "{:user/id \"alice\" :user/interested-in :concept/clojure :user/learning-state {:topic :conce"},
  "26-apperception-truth-maintenance#clojure-010"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "26-apperception-truth-maintenance.md:1110",
   :preview
   "{:agent/id :agent/release-writer :agent/role \"Prepare release-readiness summaries.\" :agent"},
  "26-apperception-truth-maintenance#clojure-011"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "26-apperception-truth-maintenance.md:1194",
   :preview
   "[{:run/id #uuid \"00000000-0000-0000-0000-000000000901\" :run/agent :agent/release-writer :r"},
  "26-apperception-truth-maintenance#clojure-012"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "26-apperception-truth-maintenance.md:1280",
   :preview
   "(d/q '[:find ?strategy (count ?run) :keys strategy revision-count :in $ ?since :where [?ru"},
  "26-apperception-truth-maintenance#clojure-013"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "26-apperception-truth-maintenance.md:1336",
   :preview
   "{:strategy.proposal/id #uuid \"00000000-0000-0000-0000-000000000911\" :strategy.proposal/age"},
  "26-apperception-truth-maintenance#clojure-014"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "26-apperception-truth-maintenance.md:1424",
   :preview
   "[{:strategy/id :strategy/release-summary-v1 :strategy/status :strategy.status/superseded :"},
  "26-apperception-truth-maintenance#clojure-015"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "26-apperception-truth-maintenance.md:1551",
   :preview
   "{:policy/id :policy/org :policy/source :policy.source/organization :policy/envelope {:tool"},
  "27-stateful-ai#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "27-stateful-ai.md:82",
   :preview
   "(defn memory-turn! [conn {:keys [user-id session-id summary fact-kind fact-content confide"},
  "27-stateful-ai#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "27-stateful-ai.md:258",
   :preview
   ";; Find concepts the student hasn't mastered but has prerequisite knowledge for (d/q '[:fi"},
  "27-stateful-ai#clojure-003"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "27-stateful-ai.md:352",
   :preview
   "{:task/id #uuid \"00000000-0000-0000-0000-000000000501\" :task/type :task.type/task :task/st"},
  "27-stateful-ai#clojure-004"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "27-stateful-ai.md:501",
   :preview
   "{:task/id #uuid \"00000000-0000-0000-0000-000000000502\" :task/board {:visible? true :lane :"},
  "27-stateful-ai#clojure-005"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "27-stateful-ai.md:574",
   :preview
   "{:task/id #uuid \"00000000-0000-0000-0000-000000000501\" :task/boundary {:summary \"Data was "},
  "27-stateful-ai#clojure-006"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "27-stateful-ai.md:650",
   :preview
   "{:tool.call/id #uuid \"00000000-0000-0000-0000-000000000503\" :tool.call/task [:task/id #uui"},
  "27-stateful-ai#clojure-007"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "27-stateful-ai.md:763",
   :preview
   "{:limit.usage/id #uuid \"00000000-0000-0000-0000-000000000505\" :limit.usage/scope :limit.sc"},
  "27-stateful-ai#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "27-stateful-ai.md:870",
   :preview
   "(require '[clojure.string :as str]) (require '[datalevin.core :as d]) (def capstone-schema"},
  "28-installation-runtime#clojure-001"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "28-installation-runtime.md:83",
   :preview
   "{:aliases {:datalevin/jvm {:jvm-opts [\"--add-opens=java.base/java.nio=ALL-UNNAMED\" \"--add-"},
  "28-installation-runtime#clojure-002"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "28-installation-runtime.md:99",
   :preview
   ":jvm-opts [\"--add-opens=java.base/java.nio=ALL-UNNAMED\" \"--add-opens=java.base/sun.nio.ch="},
  "28-installation-runtime#clojure-003"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "28-installation-runtime.md:197",
   :preview
   ";; Embedded-only with Clojure CLI {:deps {org.datalevin/datalevin-embedded {:mvn/version \""},
  "28-installation-runtime#clojure-004"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "28-installation-runtime.md:328",
   :preview
   "(require '[datalevin.server :as srv]) (def server (srv/create {:host \"127.0.0.1\" :port 889"},
  "28-installation-runtime#clojure-005"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "28-installation-runtime.md:369",
   :preview
   "(require '[datalevin.core :as d]) (def conn (d/get-conn \"dtlv://datalevin:secret@localhost"},
  "28-installation-runtime#clojure-006"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "28-installation-runtime.md:479",
   :preview
   ";; save as script.clj (require '[babashka.pods :as pods]) (pods/load-pod 'huahaiy/datalevi"},
  "28-installation-runtime#clojure-007"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "28-installation-runtime.md:499",
   :preview
   "(require '[babashka.pods :as pods]) (pods/load-pod \"dtlv\") (require '[pod.huahaiy.datalevi"},
  "28-installation-runtime#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "28-installation-runtime.md:517",
   :preview
   "(d/defpodfn greeting [name] (str \"hello \" name)) (d/q '[:find ?message :where [(greeting \""},
  "29-edn-format#clojure-001"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:29",
   :preview
   "nil true false 42 3.14 \"hello\" :user/email ?name #inst \"2026-06-09T12:00:00Z\" #uuid \"00000"},
  "29-edn-format#clojure-002"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:44",
   :preview
   "[:find ?name :where [?e :user/name ?name]] {:user/email \"ada@example.com\" :user/name \"Ada\""},
  "29-edn-format#clojure-003"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "29-edn-format.md:58",
   :preview
   "{:user/name \"Ada\" :user/age 37} {:user/name \"Ada\", :user/age 37}"},
  "29-edn-format#clojure-004"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "29-edn-format.md:98",
   :preview
   ":user/email :db/valueType :db.type/string :order.status/paid :eav :closed :string :long"},
  "29-edn-format#clojure-005"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "29-edn-format.md:116",
   :preview
   ":account/email :account/created-at :invoice/total :invoice/customer"},
  "29-edn-format#clojure-006"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:128",
   :preview
   "[:find ?e :where [?e :user/email]] [:closed \"a\" \"m\"] [:put \"users\" \"u1\" {:name \"Ada\"} :str"},
  "29-edn-format#clojure-007"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "29-edn-format.md:144",
   :preview ":user/email ;; keyword \"user/email\" ;; string"},
  "29-edn-format#clojure-008"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:157",
   :preview "?e ?name parent fulltext"},
  "29-edn-format#clojure-009"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:167",
   :preview
   "[:find ?name :where [?e :user/email \"ada@example.com\"] [?e :user/name ?name]]"},
  "29-edn-format#clojure-010"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:178",
   :preview
   "[(active? ?e)] [(> ?age 18)] [(fulltext $ \"notes\" \"error budget\") [[?e ?a ?v]]]"},
  "29-edn-format#clojure-011"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "29-edn-format.md:186",
   :preview "(d/q '[:find ?name :where [?e :user/name ?name]] db)"},
  "29-edn-format#clojure-012"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "29-edn-format.md:208",
   :preview
   "[:find ?e :where [?e :user/email]] [:db/add 42 :user/name \"Ada\"] [:user/email \"ada@example"},
  "29-edn-format#clojure-013"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:230",
   :preview
   "(parent ?child ?parent) [(parent ?child ?parent) [?child :person/parent ?parent]] [(> ?age"},
  "29-edn-format#clojure-014"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "29-edn-format.md:248",
   :preview
   "{:user/email {:db/valueType :db.type/string :db/unique :db.unique/identity} :user/name {:d"},
  "29-edn-format#clojure-015"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:267",
   :preview "#{:admin :editor}"},
  "29-edn-format#clojure-016"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:282",
   :preview "#inst \"2026-06-09T12:30:00Z\""},
  "29-edn-format#clojure-017"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:288",
   :preview "#uuid \"00000000-0000-0000-0000-000000000001\""},
  "29-edn-format#clojure-018"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "29-edn-format.md:296",
   :preview
   "{:event/id {:db/valueType :db.type/uuid :db/unique :db.unique/identity} :event/created-at "},
  "29-edn-format#clojure-019"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "29-edn-format.md:316",
   :preview
   "{:user/email {:db/valueType :db.type/string :db/unique :db.unique/identity} :user/age {:db"},
  "29-edn-format#clojure-020"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "29-edn-format.md:331",
   :preview
   "{:user/email \"ada@example.com\" :user/name \"Ada\" :user/tags #{:admin :researcher}}"},
  "29-edn-format#clojure-021"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "29-edn-format.md:344",
   :preview
   "[:db/add 42 :user/name \"Ada\"] [:db/retract 42 :user/old-email \"ada@old.example\"] [:db/retr"},
  "29-edn-format#clojure-022"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:357",
   :preview
   "[:user/email \"ada@example.com\"] [:task/id #uuid \"00000000-0000-0000-0000-000000000501\"]"},
  "29-edn-format#clojure-023"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:370",
   :preview
   "[:find ?e ?name :where [?e :user/email ?email] [?e :user/name ?name] :in $ ?email]"},
  "29-edn-format#clojure-024"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "29-edn-format.md:380",
   :preview
   "{:find '[?e ?name] :in '[$ ?email] :where '[[?e :user/email ?email] [?e :user/name ?name]]"},
  "29-edn-format#clojure-025"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "29-edn-format.md:394",
   :preview
   "[[(active? ?e) [?e :user/status :user.status/active]] [(can-login? ?e) (active? ?e) [?e :u"},
  "29-edn-format#clojure-026"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "29-edn-format.md:410",
   :preview
   "{:doc/id \"profile-1\" :doc/content {:profile {:name \"Ada\" :age 37 :tags #{:math :computing}"},
  "29-edn-format#clojure-027"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "29-edn-format.md:444",
   :preview
   "{:task/id #uuid \"00000000-0000-0000-0000-000000000501\" :task/status :task.status/running :"},
  "29-edn-format#clojure-028"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:459",
   :preview
   "(require '[clojure.edn :as edn]) (edn/read-string \"{:user/name \\\"Ada\\\"}\")"},
  "29-edn-format#clojure-029"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "29-edn-format.md:470",
   :preview
   "(pr-str {:user/name \"Ada\" :user/active? true}) ;; => \"{:user/name \\\"Ada\\\", :user/active? t"},
  "29-edn-format#clojure-030"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:484",
   :preview
   ";; EDN {:user/email \"ada@example.com\"} ;; JSON, not EDN {\"user/email\": \"ada@example.com\"}"},
  "29-edn-format#clojure-031"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:494",
   :preview
   ";; Keyword attribute :user/email ;; String \"user/email\""},
  "29-edn-format#clojure-032"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:504",
   :preview
   "#{:read :write} ;; set {:read :write} ;; map from :read to :write"},
  "29-edn-format#clojure-033"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "29-edn-format.md:511",
   :preview
   ";; Correct: pass query data. (d/q '[:find ?e :where [?e :user/email]] db) ;; Incorrect: Cl"},
  "29-edn-format#clojure-034"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:521",
   :preview "nil"},
  "29-edn-format#clojure-035"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "29-edn-format.md:527",
   :preview
   "#uuid \"00000000-0000-0000-0000-000000000001\" ;; UUID value \"00000000-0000-0000-0000-000000"},
  "30-datalog-schema-reference#clojure-001"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "30-datalog-schema-reference.md:19",
   :preview
   "(def schema {:user/email {:db/valueType :db.type/string :db/unique :db.unique/identity :db"},
  "30-datalog-schema-reference#clojure-002"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "30-datalog-schema-reference.md:33",
   :preview "(def conn (d/get-conn \"/data/app\" schema))"},
  "30-datalog-schema-reference#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "30-datalog-schema-reference.md:39",
   :preview "(d/schema conn)"},
  "30-datalog-schema-reference#clojure-004"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "30-datalog-schema-reference.md:57",
   :preview
   "(d/update-schema conn schema-update) (d/update-schema conn schema-update #{:old/attr}) (d/"},
  "30-datalog-schema-reference#clojure-005"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "30-datalog-schema-reference.md:94",
   :preview
   "(d/get-conn \"/data/app\" schema {:validate-data? true :closed-schema? true})"},
  "30-datalog-schema-reference#clojure-006"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "30-datalog-schema-reference.md:130",
   :preview "[:db/ensure 'my.app.payments/bank-account? entity]"},
  "30-datalog-schema-reference#clojure-007"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "30-datalog-schema-reference.md:183",
   :preview
   "{:user/email {:db/valueType :db.type/string :db/unique :db.unique/identity}} (d/pull db '["},
  "30-datalog-schema-reference#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "30-datalog-schema-reference.md:199",
   :preview "(d/transact! conn [{:db/ident :order.status/shipped}])"},
  "30-datalog-schema-reference#clojure-009"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "30-datalog-schema-reference.md:208",
   :preview
   "{:order/customer {:db/valueType :db.type/ref} :order/items {:db/valueType :db.type/ref :db"},
  "30-datalog-schema-reference#clojure-010"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "30-datalog-schema-reference.md:232",
   :preview
   "{:order/customer {:db/valueType :db.type/ref} :order/date {:db/valueType :db.type/instant}"},
  "30-datalog-schema-reference#clojure-011"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "30-datalog-schema-reference.md:249",
   :preview
   "{:point/xy {:db/valueType :db.type/tuple :db/tupleType :db.type/double}}"},
  "30-datalog-schema-reference#clojure-012"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "30-datalog-schema-reference.md:256",
   :preview
   "{:artist/name+country {:db/valueType :db.type/tuple :db/tupleTypes [:db.type/string :db.ty"},
  "30-datalog-schema-reference#clojure-013"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "30-datalog-schema-reference.md:273",
   :preview
   "{:post/body {:db/valueType :db.type/string :db/fulltext true :db.fulltext/domains [\"posts\""},
  "30-datalog-schema-reference#clojure-014"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "30-datalog-schema-reference.md:302",
   :preview
   "{:item/embedding {:db/valueType :db.type/vec :db.vec/domains [\"items\"]}}"},
  "30-datalog-schema-reference#clojure-015"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "30-datalog-schema-reference.md:314",
   :preview
   "{:doc/text {:db/valueType :db.type/string :db/embedding true :db.embedding/domains [\"docs\""},
  "30-datalog-schema-reference#clojure-016"
  {:status :api-sketch,
   :reason
   "Reference data, configuration, schema, or API surface snippet rather than a standalone executable example.",
   :source "30-datalog-schema-reference.md:337",
   :preview
   "{:doc/json {:db/valueType :db.type/idoc :db/idocFormat :json :db/domain \"profiles\"} :doc/e"},
  "30-datalog-schema-reference#clojure-017"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "30-datalog-schema-reference.md:361",
   :preview
   "(d/update-schema conn {:user/age {:db/valueType :db.type/long}}) (d/update-schema conn {:n"},
  "30-datalog-schema-reference#clojure-018"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "30-datalog-schema-reference.md:401",
   :preview
   "(def schema {:user/id {:db/valueType :db.type/uuid :db/unique :db.unique/identity :db/doc "},
  "31-datalog-built-ins-reference#clojure-001"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "31-datalog-built-ins-reference.md:24",
   :preview "[(> ?age 18)] [(like ?name \"A%\")]"},
  "31-datalog-built-ins-reference#clojure-002"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "31-datalog-built-ins-reference.md:31",
   :preview
   "[(+ ?subtotal ?tax) ?total] [(get-some $ ?e :profile/email :user/email) [?attr ?email]]"},
  "31-datalog-built-ins-reference#clojure-003"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "31-datalog-built-ins-reference.md:38",
   :preview
   "[:find ?city (count ?e) :where [?e :user/city ?city] :having [(> (count ?e) 10)]]"},
  "31-datalog-built-ins-reference#clojure-004"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "31-datalog-built-ins-reference.md:50",
   :preview "[(.toUpperCase ?name) ?upper]"},
  "31-datalog-built-ins-reference#clojure-005"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "31-datalog-built-ins-reference.md:152",
   :preview
   ";; Bind each vowel from a literal collection. (d/q '[:find ?vowel :where [(ground [:a :e :"},
  "31-datalog-built-ins-reference#clojure-006"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "31-datalog-built-ins-reference.md:206",
   :preview
   ";; Bind either the user's height or a default. (d/q '[:find ?e ?height :where [?e :user/na"},
  "31-datalog-built-ins-reference#clojure-007"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "31-datalog-built-ins-reference.md:231",
   :preview
   "(d/q '[:find ?e ?age :where [(q (quote [:find (min ?age) :where [_ :user/age ?age]]) $) [["},
  "31-datalog-built-ins-reference#clojure-008"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "31-datalog-built-ins-reference.md:246",
   :preview
   ";; Search all full-text domains. (d/q '[:find ?e ?a ?v :in $ ?query :where [(fulltext $ ?q"},
  "31-datalog-built-ins-reference#clojure-009"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "31-datalog-built-ins-reference.md:272",
   :preview
   ";; Vector query against an attribute-specific vector domain. (d/q '[:find ?e ?a ?v :in $ ?"},
  "31-datalog-built-ins-reference#clojure-010"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "31-datalog-built-ins-reference.md:294",
   :preview
   ";; Search idoc documents by query map. (d/q '[:find ?e ?a ?doc :where [(idoc-match $ {:sta"},
  "31-datalog-built-ins-reference#clojure-011"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "31-datalog-built-ins-reference.md:313",
   :preview
   "(d/q '[:find ?normalized :in $ ?email :where [(udf :normalize-email ?email) ?normalized]] "},
  "31-datalog-built-ins-reference#clojure-012"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "31-datalog-built-ins-reference.md:348",
   :preview
   "(d/q '[:find ?name (+ (sum ?debits) (sum ?credits)) :in [[?name ?debits ?credits]]] rows)"},
  "31-datalog-built-ins-reference#clojure-013"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "31-datalog-built-ins-reference.md:360",
   :preview
   "(d/q '[:find ?group (aggregate ?agg ?x) :in [[?group ?x]] ?agg] rows my-aggregate-fn)"},
  "34-client-api-reference#clojure-001"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "34-client-api-reference.md:27",
   :preview
   "(require '[datalevin.client :as cl]) (def client (cl/new-client \"dtlv://admin:secret@db-ho"},
  "34-client-api-reference#clojure-002"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "34-client-api-reference.md:64",
   :preview
   "(def client (cl/new-client \"dtlv://admin:secret@db-host:8898\" {:pool-size 8 :time-out 1200"},
  "34-client-api-reference#clojure-003"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "34-client-api-reference.md:106",
   :preview
   "(cl/create-database client \"app\" :datalog) (cl/open-database client \"app\" \"datalog\" {:user"},
  "34-client-api-reference#clojure-004"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "34-client-api-reference.md:182",
   :preview
   "(cl/create-user client \"alice\" \"initial-password\") (cl/create-role client :app.role/reader"},
  "34-client-api-reference#clojure-005"
  {:status :external,
   :reason
   "Requires a particular runtime, server, network service, credential, operator path, or external integration.",
   :source "34-client-api-reference.md:198",
   :preview
   "(cl/create-role client :app.role/admin) (cl/grant-permission client :app.role/admin :datal"},
  "34-client-api-reference#clojure-006"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "34-client-api-reference.md:226",
   :preview "(cl/replica-status client \"app\")"},
  "34-client-api-reference#clojure-007"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "34-client-api-reference.md:246",
   :preview
   "(cl/ha-update-membership! client \"app\" {:ha-members [{:node-id 1 :endpoint \"node-a:8898\"} "},
  "34-client-api-reference#clojure-008"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "34-client-api-reference.md:281",
   :preview
   "(cl/query-system client '[:find ?user :where [?u :user/name ?user]])"},
  "34-client-api-reference#clojure-009"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "34-client-api-reference.md:290",
   :preview
   "(def clients (cl/show-clients client)) ;; client-id must be a java.util.UUID value returne"},
  "preface#clojure-001"
  {:status :fragment,
   :reason
   "Conservative default until a maintainer marks this snippet runnable with a fixture.",
   :source "preface.md:48",
   :preview "[:find ?person :where [?person :person/name \"Alice\"]]"},
  "preface#clojure-002"
  {:status :fragment,
   :reason
   "Illustrative chapter code that depends on surrounding setup, prior snippets, expected-output comments, or fixtures.",
   :source "preface.md:191",
   :preview
   "(require '[datalevin.core :as d]) (def schema {:person/name {:db/unique :db.unique/identit"}}}
